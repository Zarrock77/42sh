/*
** EPITECH PROJECT, 2022
** my_putnbr_base_hexa
** File description:
** convert decimal to hexadecimal
*/

/*
#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

void my_putchar(char c);
int my_putstr(char *c);

int my_putnbr_base_hexa_min_for_p(long int nbr)
{
    char *hexa_base = "0123456789abcdef";
    char result[13];
    int i = 0;
    int count = 0;

    /*while ((nbr / 16) > 0 || i >= 8) {
        result[i] = hexa_base[nbr % 16];
        nbr = nbr / 16;
        i--;
        }
    result[12] = '\0';
    //i = i - 1;
    //result[i] = hexa_base[(nbr % 16)];
    for (;nbr > 0; i++) {
        result[i] = hexa_base[nbr % 16];
        nbr /= 16;
        my_putchar(result[i]);
        count++;
    }
    return (count);
}
*/
